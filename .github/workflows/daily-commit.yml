name: Daily Commit

on:
  schedule:
    - cron: '30 6 * * *'  # Runs daily at 6:30 UTC

permissions:
  contents: write          #  Gives the bot permission to push changes

jobs:
  auto-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Run Python script
        run: python auto_commit.py

      - name: Set up Git with realMe
        run: |
          git config --global user.name "killlercram"
          git config --global user.email "killlercram@users.noreply.github.com"
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}

      - name: Commit and push changes
        run: |
          git add .
          git commit -m "Automated commit on $(date)" || echo "No changes to commit"
          git push origin main
